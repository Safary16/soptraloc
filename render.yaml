# 🚀 Render.com - Deploy SoptraLoc v2.0 Optimizado
# Sistema de optimización de transporte de contenedores
# Con reloj en tiempo real y alertas de proximidad

services:
  # 🌐 Aplicación Web Principal
  - type: web
    name: soptraloc-production
    env: python
    pythonVersion: "3.12.6"
    repo: https://github.com/Safary16/soptraloc.git
    branch: main
    plan: free
    
    # 🔨 Comandos de construcción
    buildCommand: ./build.sh

    # 🚀 Pre-deploy: Migraciones y datos iniciales
    preDeployCommand: |
      cd soptraloc_system
      python manage.py migrate --settings=config.settings_production
      python manage.py reset_test_data --keep-containers 10 --settings=config.settings_production || true
      python manage.py reset_to_initial_state --keep 10 --settings=config.settings_production || true
      echo "✅ Base de datos lista con 10 contenedores de prueba"
      
    # 🚀 Comando de inicio
    startCommand: |
      cd soptraloc_system && gunicorn --bind=0.0.0.0:$PORT --timeout 600 --workers 2 --threads 4 config.wsgi:application
      
    # 🔧 Variables de entorno
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings_production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: soptraloc-production-db
          property: connectionString
      - key: ALLOWED_HOSTS
        value: "*.onrender.com,localhost,127.0.0.1"
      - key: RENDER_EXTERNAL_HOSTNAME
        value: "soptraloc-production.onrender.com"
      - key: DEPLOYMENT_ENV
        value: "production"
      - key: SYSTEM_VERSION
        value: "v2.0-optimized"
      - key: TZ
        value: "America/Santiago"

# 🗄️ Base de Datos PostgreSQL - Producción
databases:
  - name: soptraloc-production-db
    databaseName: soptraloc_prod
    user: soptraloc_prod_user
    plan: free
    
    # Inicialización post-creación
    postCreateCommand: |
      echo "✅ Base de datos PostgreSQL creada"