# 🚀 Render.com - Deploy SoptraLoc v2.0 Optimizado
# Sistema de optimización de transporte de contenedores
# Con reloj en tiempo real y alertas de proximidad
# ACTUALIZACIÓN sobre servicio existente: soptraloc.onrender.com

services:
  # 🌐 Aplicación Web Principal
  - type: web
    name: soptraloc
    env: python
    pythonVersion: "3.12.6"
    repo: https://github.com/Safary16/soptraloc.git
    branch: main
    plan: free
    
    # 🔨 Comandos de construcción
    buildCommand: ./build.sh

    # 🚀 Pre-deploy: Migraciones SOLO (conserva datos existentes)
    preDeployCommand: |
      cd soptraloc_system
      python manage.py migrate --settings=config.settings_production
      echo "✅ Migraciones aplicadas - datos existentes preservados"
      
    # 🚀 Comando de inicio
    startCommand: |
      cd soptraloc_system && gunicorn --bind=0.0.0.0:$PORT --timeout 600 --workers 2 --threads 4 config.wsgi:application
      
    # 🔧 Variables de entorno
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.6"
      - key: DEBUG
        value: "False"
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings_production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: soptraloc-production-db
          property: connectionString
      - key: ALLOWED_HOSTS
        value: "*.onrender.com,localhost,127.0.0.1"
      - key: RENDER_EXTERNAL_HOSTNAME
        value: "soptraloc.onrender.com"
      - key: DEPLOYMENT_ENV
        value: "production"
      - key: SYSTEM_VERSION
        value: "v2.0-optimized"
      - key: TZ
        value: "America/Santiago"